services:
  clipflan:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATA_DIR: /app/data
    volumes:
      - clipflan-data:/app/data
    restart: unless-stopped

volumes:
  clipflan-data:
